`docker build -t consoleApp .` build container by docker
`docker-compose up` run console app
`docker-compose up --build` build container before run
`docker-compose down -v` remove containers with created volume